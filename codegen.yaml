schema: https://graphqlzero.almansi.me/api
documents: ["**/*.ts", "**/*.tsx"]
generates:
  types/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
